import 'package:flutter/widgets.dart';

/// Icons from the bundled WingedAnt icon font (see pubspec fonts section).
class AppIcons {
  AppIcons._();

  /// Winged queen-ant silhouette (Halictus rubicundus), used for the report
  /// action and size pickers.
  static const IconData wingedAnt = IconData(0xe801, fontFamily: 'WingedAnt');
}
