# Cline Rules — Ant Nuptial Flight Predictor

Project context and guardrails for Cline (AI coding agent) sessions.

## What this is
A Flutter app (Android / iOS / Web) that downloads weather at the user's
location and predicts the likelihood that queen ants are on a nuptial
flight today and for the next week. Also has an Android home-screen
widget and background-fetch notifications.

Repo: https://github.com/bradrushworth/nuptialflight
Web: https://nuptialflight.app/

## Build / tooling notes (IMPORTANT — read before running commands)
- Flutter SDK lives at `C:/Users/Brad/flutter` (NOT the pub cache).
- The agent's *primary working directory* is often a pub-cache `android`
  folder. ALWAYS target the real project
  `C:/Users/Brad/StudioProjects/nuptialflight` for git/flutter.
- `git` must be invoked as
  `git -C "C:/Users/Brad/StudioProjects/nuptialflight" ...`
  because the shell CWD is not the project.
- `flutter analyze` on the whole project takes ~2 min the first time
  (resolves packages). Run it from the project ROOT, not a sub-path,
  otherwise it resolves paths against the wrong CWD and errors with
  "You provided the path '...' however it does not exist on disk".
- Requires `assets/.env` with `OPENWEATHERMAP_API_KEY=<key>`.

## Key source files
- `lib/main.dart` — app entry (`main()`), `MyHomePage` / `_MyHomePageState`.
  Owns the first-page load flow: `_loadData()` -> `_getLocation()` ->
  `_getWeather()` -> `_updateWeather()`. Shows a `CircularProgressIndicator`
  until `loaded == true`.
- `lib/controller/weather_fetcher.dart` — `WeatherFetcher`: location
  lookup (`findLocation`), and the 3 OpenWeatherMap calls
  (`fetchNearestWeatherLocation`, `fetchHistoricalWeather`, `fetchWeather`).
- `lib/controller/services.dart` — `initializeService()` (background_fetch
  config + notification channels), `getServicePercentage()`, `getReportedFlightsNearMe()`.
- `lib/controller/nuptials.dart` — prediction math. `Nuptials` loads two
  RandomForest models; `nuptialDailyPercentageModel` / `nuptialHourlyPercentageModel`
  score weather. The `models/*.dart` `score()` trees are GENERATED — do not hand-edit.
- `lib/controller/arangodb.dart` — ArangoDB reporting of sightings/weather.
- `lib/view/map.dart` — map page.
- `lib/responses/*.dart` — OWM response models.

## Models & feature wiring (IMPORTANT)
The two RandomForest models are GENERATED Dart (`score()` trees in
`lib/models/*.dart`), exported from the training notebooks
`lib/models/*.ipynb` (autosklearn classifiers → Dart via `m2cgen`,
→ JSON via `sklite`). Do not hand-edit the trees.

Each model expects features in a FIXED order that MUST match the training
notebook's `features.append({...})` / `X1_train.columns` order. The call sites
in `lib/controller/nuptials.dart` must pass exactly these:

- **Daily model** (`final_model.dart` / `DailyModel.score`, 15 inputs since
  2026-07-26): `[lat, lon, hemisphere, sin_doy, cos_doy, temp(day), wind,
  rain(pop), humid, cloud, press, dewPoint, dew_dep, rain1, rain2]`
- **Hourly model** (`hour_model.dart` / `HourlyModel.score`, 9 inputs — NO rain,
  NO cloud, which are commented out in `autosklearn_classification-hourly.ipynb`):
  `[lat, lon, hour, temp, wind, humid, press, dewPoint, daysSinceSpring]`

If a model is retrained with a different feature set/order, update BOTH the
call site in `nuptials.dart` AND the expectations in `test/nuptials_test.dart`,
`test/hourly_test.dart`, `test/model_test.dart`.

The per-attribute "suitability" gauges (`temperatureContribution`,
`humidityContribution`, `windContribution`, `rainContribution`,
`cloudinessContribution`, `pressureContribution`, `uviContribution`) in
`nuptials.dart` are NOT hand-tuned distributions — they are derived from
`final_model.dart` via per-feature partial-dependence curves (the model's
average flight probability as one feature varies, others held at
representative flight-season baseline contexts), normalised to [0,1].
Features the model barely uses (rain, pressure) resolve to a neutral 0.5.
`uviContribution` has no direct model input (UVI is not a feature in either
model) and delegates to the cloudiness curve via an inverse UVI→cloud map.

## First-load performance (work done 2026-07-18, commit `6123340`)
The first page was slow because the startup path blocked on serialised,
non-rendering work. Fixed:
1. `initializeService()` moved to *after* `runApp()` via `unawaited(...)`
   so background-fetch config no longer delays the first frame.
2. Notification-permission request in `_loadData()` is no longer `await`ed
   (wrapped in `unawaited(...)`) — location/weather calls start immediately.
3. `_getLocation()` now does a fast passive `getLastKnownPosition()` first
   and only falls back to an active GPS fix when none exists, avoiding the
   previous double full weather fetch on every launch. Also fixes the
   first-launch case (no cached position) which used to throw.
4. Active-GPS `timeLimit` reduced 30s -> 10s (`weather_fetcher.dart`).

Response caching is implemented in `weather_fetcher.dart` via `_fetchCached`
(`shared_preferences`, keyed by rounded lat/lon). Per-endpoint TTLs: 30 min
current/forecast, 24 h reverse geocode, 30 days historical. Repeat launches
reuse cached responses, cutting paid OWM calls.

## Verification
- `flutter analyze` reports 0 errors. The only findings are pre-existing
  `deprecated_member_use` info-warnings in `screenshots_other.dart`,
  `widgets_mobile.dart`, `utils.dart` (use of `dart:html`,
  `registerBackgroundCallback`, `canLaunch`/`launch`). Do not "fix" these
  by upgrading packages unless asked — they are out of scope and risk
  breaking the build.
- Current app version: **2.15.0+137** (retrained daily+hourly models, JSON-only forest runtime, sklite dep removed). `flutter analyze` stays at 0 errors.
- (2026-07-26) Per-attribute suitability gauges in `nuptials.dart` rewritten to
  derive from `final_model.dart` via per-feature partial-dependence curves
  (replacing the old hand-tuned Normal-CDF gauges); fixed the hourly model
  call-site mis-wire so `nuptialHourlyPercentageModel` passes the model's real
  9-feature order `[lat, lon, hour, temp, wind, humid, press, dewPoint,
  daysSinceSpring]`. `flutter analyze` stays at 0 errors; `nuptials_test.dart`,
  `hourly_test.dart`, `model_test.dart` all pass.

## Guardrails
- Keep first-page load fast: do not re-introduce `await` on
  `initializeService()` or the notification permission before `runApp()`.
- Do not hand-edit the generated `models/*.dart` score trees.
- Prefer `git -C` and verify with `flutter analyze` before claiming done.
- `flutter pub upgrade` has been observed to HANG for many minutes
  (stuck resolving git deps). If it does not finish quickly, kill the
  `dart.exe` processes and use `flutter pub get` instead to reconcile
  `pubspec.lock`. Note: `flutter pub get` may print a non-fatal
  Windows error about deleting `windows/flutter/ephemeral/.plugin_symlinks`
  — that is a desktop-build dir permission quirk and is safe to ignore.
- README.md was expanded (2026-07-18) with features, data flow,
  setup/.env template, and project structure — keep it in sync if you
  add user-facing features or change required API keys.
- Keep the model call-site feature orders in `nuptials.dart` in sync with the
  training notebooks/pipeline. The hourly model uses 9 features (no rain/
  cloud); the daily uses 15 as of the 2026-07-26 retrain (see Resumable
  work below). A mismatched order silently produces
  wrong predictions — the hourly call site was mis-wired until 2026-07-26.

## Resumable work — model retrain SHIPPING IN PROGRESS (2026-07-26, part 2)
User APPROVED shipping the improved daily model (compact retrain) and asked
for more: JSON-only runtime, hourly retrain, test-fixture redesign, notebook
updates. Working tree is DIRTY (uncommitted). Resume from "Task queue" below.

### DONE in working tree (uncommitted)
- **Ship daily model** = RF(24 trees, max_features=sqrt, max_leaf_nodes=128,
  min_samples_leaf=5, class_weight=balanced_subsample, random_state=42),
  15 features `[lat, lon, hemisphere, sin_doy, cos_doy, day(temp), windSpeed,
  rain0, humid, cloud, press, dewPoint, dew_dep, rain1, rain2]`.
  Metrics: **AUC 0.6430, AP 0.0870** (vs prod-baseline 0.500/0.048; full
  150-tree improved 0.663/0.110). 6,120 nodes. Sweep history: 32x128 AUC
  .6433; 24x128 .6430 (chosen); 20x128 .6413; 16x128 .6384.
- `lib/models/final_model.dart` REPLACED (721 KB m2cgen export + header);
  `assets/final_model.json` REPLACED (398 KB sklite). Old prod files only in
  git history (HEAD).
- `nuptials.dart`: `nuptialDailyPercentageModel(lat, lon, daily, {nocturnal,
  pop1, pop2})` now passes the 15-feature order; sin/cos use `365.25`;
  dayOfYear from UTC dt; rain1/rain2 = pop1/pop2 ?? 0.0 (matches training
  fillna(0)). `_buildPdContexts` emits 15-elem vectors (anchors carry
  dayOfYear now, sin/cos computed inline); `_PdCurve` indices: temp=5,
  wind=6, rain=7, humid=8, cloud=9, press=10.
- `main.dart` + `services.dart` call sites pass pop1/pop2 from
  daily[i+1]/[i+2].pop (guarded by length).
- Tests: `model_test.dart` rewritten (15 inputs, expects ~0.48);
  `production_model_parity_test.dart` NEW (final_model.dart score vs
  `%TEMP%/ship_expected.json`, max err 2e-14, self-skips w/o TEMP);
  `improved_model_parity_test.dart` (150-tree JSON walker, float32 cast);
  `nuptials_test.dart` gauge + Model expectations recalibrated
  (`%TEMP%/recal.py`); ALL nuptials/model/hourly/parity tests pass.

### Additional DONE (part 3, uncommitted)
- **JSON-only runtime SHIPPED**: `lib/models/forest_model.dart` NEW
  (pure-Dart `ForestModel`/`DecisionTree` predict_proba walker over the
  sklite JSON). Generated `lib/models/final_model.dart` + `hour_model.dart`
  DELETED (-1.3 MB). `sklite` dependency REMOVED from pubspec (pub get run).
  `Nuptials` now: `static Future<void> ensureLoaded()` (parses both JSON
  assets once via rootBundle), `loadFromStrings()` test hook, `.daily`/
  `.hourly` getters. main.dart: `unawaited(Nuptials.ensureLoaded())` after
  runApp + added to the `_getWeather()` Future.wait; services.dart
  `getServicePercentage()` awaits ensureLoaded before scoring.
- **Hourly model RETRAINED + SHIPPED**: `assets/hour_model.json` replaced
  (397 KB sklite; old 226 KB). RF(24, sqrt, max_leaf_nodes=128,
  min_samples_leaf=5, balanced_subsample, random_state=42) on 207,101 rows
  (4.73% pos): **AUC 0.6676, AP 0.0927**. 12 features:
  `[lat, lon, hemisphere, sin_doy, cos_doy, hour, temp, windSpeed, humid,
  press, dewPoint, dew_dep]` (no rain/cloud, as before; hour = UTC 0-23).
  Call site `nuptialHourlyPercentageModel` rewritten accordingly.
- Tests all rewritten to ForestModel-over-JSON: `model_test.dart` (0.48),
  `hourly_test.dart` (12 feats, 0.34), `production_model_parity_test.dart`
  (BOTH models vs `%TEMP%/ship_expected.json` + `ship_hour_expected.json`;
  max err 2e-14 / 1e-14), `nuptials_test.dart` (setUpAll loads models via
  `Nuptials.loadFromStrings` + File IO; Hourly Model expect 0.34).
  **45/45 pass**; `dart analyze lib test` = only the 4 pre-existing infos.
- Hourly TEMP artifacts: `hourly_cache.csv` (207,101 rows),
  `hourly_df.pkl`, `hourly_features.json`, `ship_hour_model.pkl/.dart/.json`,
  `ship_hour_expected.json`; scripts `fetch_hourly_chunk.py` (+ state),
  `hourly_train.py`, `hourly_ship.py`.

### Task queue (user-requested, NOT started/incomplete)
1. ~~JSON-only runtime~~ DONE (see part 3 above). Was: (user: "replace the dart files with the json files
   if they do the same thing"): replace generated `lib/models/*.dart` score
   trees with a small predict_proba tree-walker over the sklite JSON assets
   (walker logic proven in the parity tests; sklite pkg predict() is
   majority-vote only — NOT usable). ⚠️ `_PdCurve`/score are currently
   synchronous; asset loading is async — needs an await-able init (e.g.
   `Nuptials.ensureLoaded()`) before first score; tests must load JSON from
   file system not rootBundle (or use TestWidgetsFlutterBinding + asset).
2. ~~Hourly retrain~~ DONE (see part 3 above). Was: with same improvements. BLOCKER found: first-doc probe
   showed `weather.hourly == null` (len 0) — count docs with hourly != null
   first (`%TEMP%/probe_hourly.py`); old notebook filtered on it. Fetch via
   resumable chunked AQL projection; features analogous to daily (+hour).
3. ~~Test-fixture redesign~~ DONE (part 3): fixtures rebuilt from the PD
   optima (Perfect 30C/1.5ms/67%/cloud95/1035hPa ... Worst 3C/30ms). Model
   scores now rank Perfect .60 > Great .55 > Ordinary .50 > Bad .41 >
   Worst .01; gauges recalibrated (`%TEMP%/refix.py` + `recal2.py`);
   41/41 nuptials tests pass. Was: (user: make good/perfect/bad day tests reflect
   discovered patterns). PD sweep of ship model (`%TEMP%/pd_sweep.py`):
   temp BEST 34C/WORST 0C (span .094); wind BEST 1.5/WORST 12.5 m/s (.057);
   press BEST 1038.5/WORST 1004 hPa (.038); humid BEST 67.5/WORST 95 (.021);
   cloud BEST 97.5/WORST 2.5 (.018); rain0 near-flat (.009). Redesign
   fixtures around these + rerun recalibration.
4. ~~Notebook updates~~ DONE: both ipynbs REWRITTEN from scratch (6 cells
   each, no outputs, ~8 KB vs old 580 KB autosklearn relics): projected
   AQL fetch, engineered features, compact RF config, sklite export +
   parity fixtures, shipping checklist. Was:: `lib/models/autosklearn_classification.ipynb` (71
   cells; cell 5 = old docker-host fetch `RETURN {f}`) + hourly ipynb —
   update to: TLS host, projected AQL, engineered features, the ship-model
   RF config, m2cgen/sklite export, and note the ccp_alpha finding.
5. ~~Wrap-up~~ DONE: findings doc has a SHIPPED addendum; README table
   now daily-15/hourly-12 + forest_model.dart runtime; version bumped to
   2.15.0+137; flutter analyze = 4 pre-existing infos only; all model/
   nuptials/widget tests pass (full `flutter test` exceeds the 30s shell
   cap on the network tests - run suites individually). Was:: update `docs/model_training_findings.md` + README (currently
   say "NOT shipped" — now shipped), bump version in pubspec.yaml
   (currently 2.14.1+136), full `flutter analyze` + `flutter test`, commit.

### Key TEMP artifacts (`%TEMP%` = C:\Users\Brad\AppData\Local\Temp)
- `ship_model.pkl/.dart/.json`, `ship_expected.json`, `ship_train.py` (the
  shipped compact model + parity fixtures; retrain reproducible via
  random_state=42 on df.pkl).
- `df.pkl` (212,504 rows engineered), `flights_cache.csv` (raw fetch),
  `features.json` ({base:10, imp:15}), `holdout.pkl`,
  `improved_model.pkl` (150 trees), `baseline_model.pkl` (degenerate),
  `final_model_improved.json/.dart`, `expected.json`.
- Scripts: `fetch_chunk.py` (resumable AQL fetch), `prep.py`,
  `train_compact*.py`, `ship_train.py`, `calc_expect.py`, `recal.py`,
  `pd_sweep.py`, `probe_hourly.py`, `export_dart_chunk.py`.
- If TEMP was wiped: re-fetch with fetch_chunk.py loop, prep.py, then
  ship_train.py (deterministic, random_state=42).

### DB / security
- ArangoDB `https://api.bitbot.com.au:8530`, db `nuptialFlight`, user
  `notebook` (creds in the ipynb). AQL projection is ~30x faster.
- Defaced collection "u have been pwned bro…" is EMPTY; `notebook` and
  `nuptialflight` users both get HTTP 403 on drop (need admin/root — user
  said auth was added server-side; deletion still pending admin creds).
### Verify before claiming done
`dart analyze lib/controller/nuptials.dart test/*.dart` (no issues);
`flutter test test/nuptials_test.dart test/hourly_test.dart test/model_test.dart`
(all pass); `flutter analyze` (only the 4 pre-existing
`deprecated_member_use` infos).
