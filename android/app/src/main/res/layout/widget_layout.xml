<!-- Full widget: heading, Ant Flight Index band, calibrated odds. The band
     colour arrives via the background drawable + fg text colour set from
     AppWidgetProvider; text content is pre-localized by the Flutter side. -->
<FrameLayout
    android:id="@+id/widget_root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/linear_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/widget_padding"
        android:background="@drawable/widget_bg_neutral">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Wingless worker on quiet days, winged queen when a flight is
                 likely; AppWidgetProvider swaps the drawable and tints it to
                 the band colour. Decorative: tv_band carries the meaning. -->
            <ImageView
                android:id="@+id/iv_ant"
                android:layout_width="24dp"
                android:layout_height="12dp"
                android:scaleType="fitCenter"
                android:adjustViewBounds="true"
                android:src="@drawable/widget_ant_worker"
                android:importantForAccessibility="no" />

            <TextView
                android:id="@+id/tv_heading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:gravity="center"
                android:maxLines="1"
                android:text="@string/widget_heading"
                android:textColor="@color/widget_text_secondary"
                android:letterSpacing="0.08"
                android:textAllCaps="true"
                android:textSize="10sp" />

        </LinearLayout>

        <TextView
            android:id="@+id/tv_band"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:text=""
            android:textColor="@color/widget_text"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/tv_odds"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:gravity="center"
            android:maxLines="1"
            android:text=""
            android:textColor="@color/widget_text_secondary"
            android:textSize="12sp" />

    </LinearLayout>
</FrameLayout>
