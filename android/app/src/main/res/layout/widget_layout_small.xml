<!-- Compact (1-cell) widget: the ant glyph (worker or winged queen,
     depending on the band) and the band word, on the band colour.
     Chosen via RemoteViews size mapping on Android 12+. -->
<FrameLayout
    android:id="@+id/widget_root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/linear_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="4dp"
        android:background="@drawable/widget_bg_neutral">

        <ImageView
            android:id="@+id/iv_ant"
            android:layout_width="32dp"
            android:layout_height="16dp"
            android:layout_marginBottom="1dp"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            android:src="@drawable/widget_ant_worker"
            android:importantForAccessibility="no" />

        <TextView
            android:id="@+id/tv_band"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:text=""
            android:textColor="@color/widget_text"
            android:textSize="13sp" />

    </LinearLayout>
</FrameLayout>
